node_modules/
dist/
out/
*.log
.DS_Store
Thumbs.db

# IDE / editor state
.idea/
.vscode/
*.iml

# Agent tooling / local notes
.artifacts/
# Owner task ledger — references local tooling, keep it out of the public repo.
TODO.md

# Windows NTFS alternate data streams (downloaded-file markers)
*:Zone.Identifier

# OS / environment files
.env
.env.*

# Agent workspace state (local tooling, not part of the app)
.freebuff/

# Runtime artifacts the fake kimi fixture regenerates on every test run
# (device ids, cached client config, update probes, workspace registry)
test-fixtures/kimi-home/cache/
test-fixtures/kimi-home/updates/
test-fixtures/kimi-home/device_id
test-fixtures/kimi-home/tui.toml
test-fixtures/kimi-home/workspaces.json
debug-boot.log
